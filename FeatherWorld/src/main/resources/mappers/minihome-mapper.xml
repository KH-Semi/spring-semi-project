<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.featherworld.project.miniHome.model.mapper.MiniHomeMapper">

	<!-- 파일 정보를 DB에 삽입 -->
	<insert id="insertUploadFile">
		INSERT INTO "UPLOAD_FILE"
		VALUES(SEQ_FILE_NO.NEXTVAL, #{filePath},
		#{fileOriginalName}, #{fileRename}, DEFAILT,
		#{memberNo})
	</insert>
	
	<select id="findmember" resultType="Member">
		SELECT MEMBER_NO , MEMBER_NAME, MEMBER_EMAIL, MEMBER_IMG
		,MEMBER_INTRO,HOME_TITLE,VISITOR_TOTAL
		FROM MEMBER 
		WHERE MEMBER_NO = #{memberNo}
		AND MEMBER_DEL_FL = 'N'
	</select>
	
	<select id="findilchon" resultType="_int">
		SELECT COUNT(*)
		FROM ILCHON
		WHERE TO_MEMBER_NO = #{toMemberNo}
		AND FROM_MEMBER_NO = #{fromMemberNo}
	</select>	
</mapper>
